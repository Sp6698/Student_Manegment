# Production — set this to your deployed backend URL
# If frontend and backend are on the same server, keep /api (relative)
VITE_API_URL=/api
# If hosted separately (e.g. frontend on Vercel, backend on Render):
# VITE_API_URL=https://your-backend.onrender.com/api
